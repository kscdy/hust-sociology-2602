const KEY="hust-sociology-2602-messages";const seeds=[{name:"辅导员",body:"愿 2602 班求是、共情、同行。",time:"2026-09-01 09:00"},{name:"开发者小陈",body:"欢迎随时在这里留下想说的话。",time:"2026-09-10 21:18"}];function esc(s){return String(s).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;");}function load(){try{const p=JSON.parse(localStorage.getItem(KEY)||"[]");return Array.isArray(p)&&p.length?p:seeds;}catch{return seeds;}}function render(items){const list=document.getElementById("message-list");if(!list)return;list.innerHTML=items.slice().reverse().map(i=>`<article class="message-card"><strong>${esc(i.name)}</strong><p>${esc(i.body)}</p><time>${esc(i.time)}</time></article>`).join("");}let messages=load();render(messages);const form=document.getElementById("message-form");if(form)form.addEventListener("submit",e=>{e.preventDefault();const d=new FormData(form);const name=String(d.get("name")||"").trim();const body=String(d.get("body")||"").trim();if(!name||!body)return;const now=new Date();const pad=n=>String(n).padStart(2,"0");messages.push({name,body,time:`${now.getFullYear()}-${pad(now.getMonth()+1)}-${pad(now.getDate())} ${pad(now.getHours())}:${pad(now.getMinutes())}`});localStorage.setItem(KEY,JSON.stringify(messages));render(messages);form.reset();});
